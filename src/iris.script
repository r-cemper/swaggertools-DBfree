zn "%SYS"
  
// Unexpire passwords and set up passwordless mode to simplify dev use.
do ##class(Security.Users).UnExpireUserPasswords("*")
zpm "install passwordless"
zpm "install iterm"

zn "USER"
write $system.OBJ.LoadDir("/ext/src/","ck")
halt

